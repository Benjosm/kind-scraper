/**
 * Checks if crawling is allowed for a given URL based on robots.txt rules.
 * @param {string} url - The URL to check.
 * @returns {Promise<boolean>} Resolves to true (stubbed).
 */
export async function checkRobots(url) {
  // TODO: Real parser
  return true;
}
